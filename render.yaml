services:
  - type: web
    name: paladins-palace
    runtime: node
    region: frankfurt
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
  buildCommand: npm ci --include=dev && npm run vercel-build
    startCommand: npm run start
    healthCheckPath: /
